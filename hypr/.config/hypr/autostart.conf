# Extra autostart processes
  exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP &
  exec-once = /usr/lib/systemd/user/xdg-desktop-portal-hyprland.service &
  exec-once = /usr/lib/systemd/user/xdg-desktop-portal.service &
  exec-once = kdeconnectd
  exec-once = espanso status; espanso restart && espanso start
  exec-once = elephant 
  exec-once = walker --gapplication-service
